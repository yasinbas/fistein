spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fistein_db
    username: postgres
    password: Ysn2025!
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  # Hata mesajlarını detaylı göster
  mvc:
    problemdetails:
      enabled: true

server:
  port: 8080

app:
  jwt:
    secret: mySecretKeyForJwtTokenGenerationMustBeAtLeast512BitsLongForHS512Algorithm
    expiration: 86400000 # 24 hours in milliseconds
